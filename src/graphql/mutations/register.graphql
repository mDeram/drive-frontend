mutation Register($inputs: RegisterInput!) {
    register(inputs: $inputs) {
        __typename
        ... on User {
            ...DefaultUser
        }
        ... on FormErrors {
            errors {
                message
                field
            }
        }
    }
}
